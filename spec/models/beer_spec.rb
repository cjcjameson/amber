require 'spec_helper'

describe Beer do
	context "search for beers, given a style" do
		

		it "responds with a beer style with beers" do
			pending
		end

		it "gives notification if none found" do
			pending
		end

		it "limits search to 20 beers" do
			pending
		end
	end
end